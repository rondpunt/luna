/**
 * Base44 edge function id voor Luna-chat (Anthropic via server).
 * Moet exact overeenkomen met de deployed function name in Base44.
 * Nog `noraChat` tot server + dashboard op `lunaChat` (of gelijkaardig) staan.
 */
export const BASE44_LUNA_CHAT_FUNCTION_ID = "noraChat";
